% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log-read.R
\name{vitals_log_read}
\alias{vitals_log_read}
\title{Read an eval log back into ellmer chats}
\usage{
vitals_log_read(path, solver_chat = NULL, scorer_chat = NULL, tools = list())
}
\arguments{
\item{path}{Path to an eval log file, e.g. an element of the output of
\code{list.files(vitals_log_dir())}.}

\item{solver_chat}{Optional. An \link[ellmer:Chat]{ellmer::Chat} object to use as the base
for reconstructed solver chats. When \code{NULL}, the chat is constructed with
\code{\link[ellmer:chat]{ellmer::chat()}} using the log's model string; supply this argument when
the log's provider requires additional configuration (e.g. a \code{base_url}).}

\item{scorer_chat}{Optional. Analogous to \code{solver_chat}, for the chats of
model-graded scorers.}

\item{tools}{Optional. A named list of \code{\link[ellmer:tool]{ellmer::tool()}} definitions to
re-register on the reconstructed chats and attach to their tool calls.}
}
\value{
A tibble with columns \code{id}, \code{epoch}, \code{input}, \code{target}, \code{result},
\code{score}, and \code{solver_chat}, mirroring the output of \link{Task}'s
\verb{$get_samples()} method. When the log contains model-graded scoring events,
a \code{scorer_chat} column is included as well.
}
\description{
Reconstruct the samples of a logged evaluation, including their
\link[ellmer:Chat]{ellmer::Chat} objects, from an eval log file written by \link{Task}'s \verb{$log()}
method (or by Python Inspect).

Chats are rebuilt from the log's message history: turn contents (text,
reasoning, images, tool calls, and tool results), per-turn token usage,
durations, and finish reasons are all restored. Information that is not
written to the log--most notably provider configuration beyond the model
name and the raw provider responses--cannot be recovered. Tool functions
are not serializable; pass \code{tools} to re-attach tool definitions by name.
}
\examples{
log_file <- system.file(
  "test/inspect/logs",
  "2025-03-24T10-39-36-05-00_simple-arithmetic_fQ9mYnqZFhtEuUenPpJgKL.json",
  package = "vitals"
)

samples <- vitals_log_read(log_file)

samples$solver_chat[[1]]

}
